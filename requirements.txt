# Shared dependencies for seo-engine skill scripts.
# Kept deliberately minimal — scripts prefer stdlib where practical.
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
google-auth>=2.28        # service-account auth for Google Search Console / Indexing APIs
google-api-python-client>=2.100  # Search Console API client
extruct>=0.18            # offline JSON-LD/Microdata extraction for schema validation
